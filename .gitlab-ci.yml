image:
  name: node:10

before_script:
  - docker rm -f test

test:
  stage: deploy
  script:
    - yarn --version
    - yarn
    - yarn run release:electron
  artifacts:
    paths:
      - $CI_PROJECT_DIR/dist/*.*